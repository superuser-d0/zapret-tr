<!-- Koyu tema. Zemin neredeyse siyah; kontroller bir ton açık, yazı kırık beyaz.

     Standart kontrollerin bir kısmı burada YENİDEN ÇİZİLİYOR, çünkü Windows'un
     (Aero2) şablonları bazı renkleri sabit yazıyor ve Background vermek onları
     değiştirmiyor: açılır listenin düğmesi gri geçişli kalıyor, onay işareti
     koyu zemin üzerinde koyu (görünmez), kaydırma çubuğu açık gri. Şablonlar
     açık temaya konmadı; açık temada Windows görünümü aynen duruyor.

     Boyutlar açık temadakiyle aynı tutuldu (açılır liste 28, Ayrıntılar başlığı
     yaklaşık 23): pencerenin varsayılan boyutta sığması temadan bağımsız olmalı. -->
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:sys="clr-namespace:System;assembly=mscorlib">

    <sys:String x:Key="ThemeName">Dark</sys:String>

    <SolidColorBrush x:Key="StatusReadyBrush"   Color="#1A2230" />
    <SolidColorBrush x:Key="StatusRunningBrush" Color="#133020" />
    <SolidColorBrush x:Key="StatusPausedBrush"  Color="#33280F" />
    <SolidColorBrush x:Key="StatusTestingBrush" Color="#172640" />
    <SolidColorBrush x:Key="StatusFaultedBrush" Color="#3A1717" />

    <SolidColorBrush x:Key="WindowBackBrush"   Color="#0E1013" />
    <SolidColorBrush x:Key="TextBrush"         Color="#E6E8EB" />
    <SolidColorBrush x:Key="TextMutedBrush"    Color="#9AA3AE" />
    <SolidColorBrush x:Key="AccentBrush"       Color="#7AB0FF" />
    <SolidColorBrush x:Key="BorderBrushSoft"   Color="#2C3139" />

    <SolidColorBrush x:Key="ControlBackBrush"  Color="#171A1F" />
    <SolidColorBrush x:Key="ControlHoverBrush" Color="#222730" />
    <SolidColorBrush x:Key="PrimaryBackBrush"  Color="#13161B" />
    <SolidColorBrush x:Key="InputBackBrush"    Color="#13161B" />
    <SolidColorBrush x:Key="InputBorderBrush"  Color="#3A404A" />
    <SolidColorBrush x:Key="SelectedBackBrush" Color="#1F3350" />
    <SolidColorBrush x:Key="LogBackBrush"      Color="#0A0C0F" />

    <SolidColorBrush x:Key="DangerTextBrush"   Color="#FF8A8A" />
    <SolidColorBrush x:Key="DangerBorderBrush" Color="#5C2A2A" />

    <SolidColorBrush x:Key="WarningBrush"       Color="#F2C46D" />
    <SolidColorBrush x:Key="WarningBackBrush"   Color="#2A2112" />
    <SolidColorBrush x:Key="WarningBorderBrush" Color="#574320" />

    <SolidColorBrush x:Key="UpdateBackBrush"    Color="#2A2112" />
    <SolidColorBrush x:Key="UpdateBorderBrush"  Color="#574320" />
    <SolidColorBrush x:Key="UpdateTextBrush"    Color="#F2C46D" />
    <SolidColorBrush x:Key="UpdateSubTextBrush" Color="#C9A866" />

    <!-- ================= Metin kutusu ================= -->
    <Style TargetType="TextBox">
        <Setter Property="Background" Value="{DynamicResource InputBackBrush}" />
        <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource InputBorderBrush}" />
        <Setter Property="CaretBrush" Value="{DynamicResource TextBrush}" />
        <Setter Property="SelectionBrush" Value="{DynamicResource AccentBrush}" />
    </Style>

    <!-- ================= Onay kutusu ================= -->
    <Style TargetType="CheckBox">
        <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CheckBox">
                    <Grid Background="Transparent">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Border x:Name="Kutu" Width="14" Height="14" CornerRadius="2"
                                VerticalAlignment="Center"
                                Background="{DynamicResource InputBackBrush}"
                                BorderBrush="{DynamicResource InputBorderBrush}"
                                BorderThickness="1">
                            <Path x:Name="Isaret" Data="M 2,6 L 5,9 L 10,3"
                                  Stroke="{DynamicResource AccentBrush}" StrokeThickness="2"
                                  HorizontalAlignment="Center" VerticalAlignment="Center"
                                  Visibility="Collapsed" />
                        </Border>
                        <ContentPresenter Grid.Column="1" Margin="6,0,0,0"
                                          VerticalAlignment="Center" RecognizesAccessKey="True" />
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="Isaret" Property="Visibility" Value="Visible" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Kutu" Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ================= Açılır liste ================= -->
    <ControlTemplate x:Key="KoyuAcilirListeDugmesi" TargetType="ToggleButton">
        <Border x:Name="Zemin" CornerRadius="3" BorderThickness="1"
                Background="{DynamicResource InputBackBrush}"
                BorderBrush="{DynamicResource InputBorderBrush}">
            <Path HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,9,0"
                  Data="M 0,0 L 4,4 L 8,0" Stroke="{DynamicResource TextMutedBrush}" StrokeThickness="1.5" />
        </Border>
        <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="Zemin" Property="Background" Value="{DynamicResource ControlHoverBrush}" />
            </Trigger>
            <Trigger Property="IsChecked" Value="True">
                <Setter TargetName="Zemin" Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <Style TargetType="ComboBox">
        <Setter Property="Height" Value="28" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled" />
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBox">
                    <Grid>
                        <ToggleButton Template="{StaticResource KoyuAcilirListeDugmesi}"
                                      Focusable="False" ClickMode="Press"
                                      IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" />
                        <ContentPresenter IsHitTestVisible="False" Margin="7,0,26,0"
                                          HorizontalAlignment="Left" VerticalAlignment="Center"
                                          Content="{TemplateBinding SelectionBoxItem}"
                                          ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                          ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                          ContentStringFormat="{TemplateBinding SelectionBoxItemStringFormat}" />
                        <Popup IsOpen="{TemplateBinding IsDropDownOpen}" Placement="Bottom"
                               AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                            <Border MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}"
                                    MaxHeight="{TemplateBinding MaxDropDownHeight}"
                                    Background="{DynamicResource ControlBackBrush}"
                                    BorderBrush="{DynamicResource InputBorderBrush}" BorderThickness="1"
                                    Margin="0,2,0,0">
                                <ScrollViewer SnapsToDevicePixels="True">
                                    <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained" />
                                </ScrollViewer>
                            </Border>
                        </Popup>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ComboBoxItem">
        <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
        <Setter Property="Padding" Value="7,4" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBoxItem">
                    <Border x:Name="Zemin" Background="Transparent" Padding="{TemplateBinding Padding}">
                        <ContentPresenter />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="Zemin" Property="Background" Value="{DynamicResource SelectedBackBrush}" />
                        </Trigger>
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter TargetName="Zemin" Property="Background" Value="{DynamicResource ControlHoverBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ================= Günlük listesi ================= -->
    <Style TargetType="ListBox">
        <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
    </Style>

    <Style TargetType="ListBoxItem">
        <Setter Property="Padding" Value="4,1" />
        <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListBoxItem">
                    <Border x:Name="Zemin" Background="Transparent" Padding="{TemplateBinding Padding}">
                        <ContentPresenter />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="Zemin" Property="Background" Value="{DynamicResource SelectedBackBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ================= Ayrıntılar paneli ================= -->
    <ControlTemplate x:Key="KoyuAyrintilarBasligi" TargetType="ToggleButton">
        <Border Background="Transparent" Padding="0,2">
            <StackPanel Orientation="Horizontal">
                <Grid Width="19" Height="19">
                    <Ellipse x:Name="Daire" Stroke="{DynamicResource TextMutedBrush}" StrokeThickness="1" />
                    <Path x:Name="Ok" Data="M 0,0 L 3.5,3.5 L 7,0" Margin="0,2,0,0"
                          Stroke="{DynamicResource TextBrush}" StrokeThickness="1.6"
                          HorizontalAlignment="Center" VerticalAlignment="Center" />
                </Grid>
                <ContentPresenter Margin="4,0,0,0" VerticalAlignment="Center" RecognizesAccessKey="True" />
            </StackPanel>
        </Border>
        <ControlTemplate.Triggers>
            <Trigger Property="IsChecked" Value="True">
                <Setter TargetName="Ok" Property="Data" Value="M 0,3.5 L 3.5,0 L 7,3.5" />
                <Setter TargetName="Ok" Property="Margin" Value="0,0,0,2" />
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="Daire" Property="Stroke" Value="{DynamicResource AccentBrush}" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <Style TargetType="Expander">
        <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Expander">
                    <DockPanel>
                        <ToggleButton DockPanel.Dock="Top"
                                      Template="{StaticResource KoyuAyrintilarBasligi}"
                                      Content="{TemplateBinding Header}"
                                      Foreground="{TemplateBinding Foreground}"
                                      IsChecked="{Binding IsExpanded, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" />
                        <ContentPresenter x:Name="Icerik" Visibility="Collapsed" Focusable="False" />
                    </DockPanel>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsExpanded" Value="True">
                            <Setter TargetName="Icerik" Property="Visibility" Value="Visible" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ================= Kaydırma çubuğu ================= -->
    <Style x:Key="KoyuKaydirmaBosluk" TargetType="RepeatButton">
        <Setter Property="Focusable" Value="False" />
        <Setter Property="IsTabStop" Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RepeatButton">
                    <Border Background="Transparent" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="KoyuKaydirmaTutamaci" TargetType="Thumb">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Thumb">
                    <Border x:Name="Tutamac" CornerRadius="4" Margin="2" Background="#3A404A" />
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Tutamac" Property="Background" Value="#4C5360" />
                        </Trigger>
                        <Trigger Property="IsDragging" Value="True">
                            <Setter TargetName="Tutamac" Property="Background" Value="#5A6270" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ScrollBar">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="MinWidth" Value="0" />
        <Setter Property="MinHeight" Value="0" />
        <Setter Property="Width" Value="12" />
        <Setter Property="Height" Value="Auto" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ScrollBar">
                    <Border Background="{TemplateBinding Background}">
                        <Track x:Name="PART_Track" IsDirectionReversed="True">
                            <Track.DecreaseRepeatButton>
                                <RepeatButton Style="{StaticResource KoyuKaydirmaBosluk}" Command="ScrollBar.PageUpCommand" />
                            </Track.DecreaseRepeatButton>
                            <Track.Thumb>
                                <Thumb Style="{StaticResource KoyuKaydirmaTutamaci}" />
                            </Track.Thumb>
                            <Track.IncreaseRepeatButton>
                                <RepeatButton Style="{StaticResource KoyuKaydirmaBosluk}" Command="ScrollBar.PageDownCommand" />
                            </Track.IncreaseRepeatButton>
                        </Track>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="Orientation" Value="Horizontal">
                <Setter Property="Width" Value="Auto" />
                <Setter Property="Height" Value="12" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ScrollBar">
                            <Border Background="{TemplateBinding Background}">
                                <Track x:Name="PART_Track" IsDirectionReversed="False">
                                    <Track.DecreaseRepeatButton>
                                        <RepeatButton Style="{StaticResource KoyuKaydirmaBosluk}" Command="ScrollBar.PageLeftCommand" />
                                    </Track.DecreaseRepeatButton>
                                    <Track.Thumb>
                                        <Thumb Style="{StaticResource KoyuKaydirmaTutamaci}" />
                                    </Track.Thumb>
                                    <Track.IncreaseRepeatButton>
                                        <RepeatButton Style="{StaticResource KoyuKaydirmaBosluk}" Command="ScrollBar.PageRightCommand" />
                                    </Track.IncreaseRepeatButton>
                                </Track>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- ScrollViewer'ın iki çubuğun kesiştiği köşesi Aero2'de açık gri kalıyor. -->
    <Style TargetType="ScrollViewer">
        <Setter Property="Background" Value="Transparent" />
    </Style>

    <!-- ================= Diğer ================= -->
    <Style TargetType="ProgressBar">
        <Setter Property="Background" Value="{DynamicResource InputBackBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource InputBorderBrush}" />
    </Style>

    <Style TargetType="ToolTip">
        <Setter Property="Background" Value="{DynamicResource ControlBackBrush}" />
        <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource InputBorderBrush}" />
    </Style>

</ResourceDictionary>
